<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cloudcare.mapper.HealthWarningMapper">

    <!-- 查询指定日期之前的健康预警数量 -->
    <select id="selectCountByWarningTimeBefore" resultType="int">
        SELECT COUNT(1) FROM health_warning
        WHERE DATE(warning_time) &lt;= #{date}
    </select>

    <!-- 根据预警级别和日期查询健康预警数量 -->
    <select id="selectCountByWarningLevelAndDate" resultType="int">
        SELECT COUNT(1) FROM health_warning
        WHERE warning_level = #{warningLevel}
        AND DATE(warning_time) = #{date}
    </select>

    <!-- 根据预警级别和日期范围查询健康预警数量 -->
    <select id="selectCountByWarningLevelAndDateRange" resultType="int">
        SELECT COUNT(1) FROM health_warning
        WHERE warning_level = #{warningLevel}
        AND DATE(warning_time) BETWEEN #{startDate} AND #{endDate}
    </select>

</mapper>